# ==================== IELTS Mock Test System Configuration ====================
#
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!
#

# ==================== Application ====================
APP_NAME=IELTS Mock Test System
DEBUG=True
HOST=0.0.0.0
PORT=8000

# ==================== Database ====================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://ielts_user:your_password_here@localhost:5432/ielts_db

# ==================== Security ====================
# CRITICAL: Generate a strong secret key for production!
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-min-32-characters-please-change-in-production-use-secrets-token-urlsafe

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== CORS ====================
# Comma-separated list of allowed origins (frontend URLs)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ==================== File Uploads ====================
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=10

# Allowed file extensions (comma-separated)
ALLOWED_AUDIO_EXTENSIONS=.mp3,.wav,.m4a,.ogg
ALLOWED_IMAGE_EXTENSIONS=.jpg,.jpeg,.png,.gif,.webp
